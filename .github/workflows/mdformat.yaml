---
name: Mdformat

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

permissions:
  contents: read

jobs:
  mdformat:
    name: Markdown formatting
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1

      - name: Install uv
        uses: astral-sh/setup-uv@c771a70e6277c0a99b617c7a806ffedaca235ff9 # v9.0.0
        with:
          python-version: '3.12'

      # Settings come from .mdformat.toml - no flags here, so CI and a local
      # run cannot disagree. The --with plugins mirror the local install; a
      # missing plugin changes what mdformat considers formatted.
      # templates/ is excluded on purpose (placeholders); templates/README.md
      # is the one real markdown file in it.
      - name: Run mdformat (check only)
        run: >
          uvx --with mdformat-gfm --with mdformat-front-matters
          --with mdformat-footnote --with mdformat-myst
          mdformat --check *.md sample_prompts/*.md .github/*.md
          .github/instructions/*.md .claude/rules/*.md templates/README.md
