<!--
  Copy to <repo-root>/CLAUDE.local.md

  THIS FILE IS NOT COMMITTED. Add it to .gitignore (see gitignore-snippet.txt).
  It loads alongside CLAUDE.md and is treated exactly the same way - the only
  difference is that it is yours.

  Only Claude Code loads this file, so it holds only Claude-Code-specific
  local notes and preferences. Machine facts every tool needs - checkout
  paths, interpreter, quirks - go in .status/local-environment.md, which this
  file imports:

      @.status/local-environment.md

  WORKTREE CAVEAT: because this file is gitignored, it does NOT exist in a new
  git worktree created by `claude --worktree`. If you use worktrees, put the
  content in a home-directory file and import it here instead:

      @~/.claude/<repo-name>-local.md

  A project-level import that resolves outside the working directory triggers a
  one-time approval dialog. Accept it once and it stops asking.
  (You can also list this file in .worktreeinclude at the repo root, which tells
  Claude Code to copy matching gitignored files into each new worktree.)
-->

# Local notes - <repo-name>

## Machine facts

@.status/local-environment.md

<!-- The import above is the single home for checkout paths, the interpreter,
     and machine quirks - every tool reads that file, not this one. Add below
     only what is both machine-specific AND Claude-Code-specific. -->

- <e.g. "Git Bash is at <path>; CLAUDE_CODE_GIT_BASH_PATH is set in user settings.">

Sibling access is granted via `.claude/settings.local.json`
(`permissions.additionalDirectories`) or `claude --add-dir <path>` at launch.

## My preferences for this repo

<!-- Things a collaborator should not inherit. -->
- <e.g. "Don't run the full validation suite unless I ask; it takes 40 minutes.">
- <e.g. "I read plans in VS Code, so write them as markdown with headings, not tables.">

## Scratch

<!-- Freeform. Half-formed thoughts, current focus, things to remember this week.
     Cheap to keep because nobody else ever sees it. Prune it occasionally -
     it loads into context every session, same as CLAUDE.md. -->
- Currently working on: <...>
